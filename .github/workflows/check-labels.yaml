name: CORE4 label enforcement
permissions:
  contents: read
  pull-requests: write
on:
  pull_request:
    types: [opened, labeled, unlabeled, synchronize, reopened, edited]
jobs:
  require-label:
    uses: guardian/.github/.github/workflows/require-label.yaml@ca52d0cb6bc832fbdfda6f5e9fb80e91bbc1783c
