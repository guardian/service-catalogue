name: CORE4 label enforcement
permissions:
  contents: read
  pull-requests: write
on:
  pull_request:
    types: [opened, labeled, unlabeled, synchronize, reopened, edited]
jobs:
  require-label:
    uses: guardian/.github/.github/workflows/require-label.yaml@ea51e21ff05e80fccaff1413ba40a4571a08c1c1
