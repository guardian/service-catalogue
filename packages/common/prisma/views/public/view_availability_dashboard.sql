SELECT
  account_id,
  account_name,
  region,
  service_name,
  load_balancer_type,
  load_balancer_arn,
  load_balancer_name,
  stack,
  stage,
  app,
  repo,
  riffraff_project,
  target_group_arn,
  target_group_origin_type,
  target_group_origin_arn,
  target_group_origin_name,
  elk_index,
  grafana_cloudwatch_datasource,
  kibana_space,
  slo,
  state_value,
  domain_names
FROM
  fn_view_availability_dashboard() fn_view_availability_dashboard(
    account_id,
    account_name,
    region,
    service_name,
    load_balancer_type,
    load_balancer_arn,
    load_balancer_name,
    stack,
    stage,
    app,
    repo,
    riffraff_project,
    target_group_arn,
    target_group_origin_type,
    target_group_origin_arn,
    target_group_origin_name,
    elk_index,
    grafana_cloudwatch_datasource,
    kibana_space,
    slo,
    state_value,
    domain_names
  );