# `CloudQuery.yaml` Files

So that we can have a multitude of configurations that we can run for local development, we now have two exemplar files that provide examples of configuring for retrieving AWS and GitHub data.

`cloudquery-gtihub.yaml` in particular includes the correct file paths for the keys and IDs, and the full config for organizations and enterprises.
