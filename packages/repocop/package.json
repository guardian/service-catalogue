{
  "name": "repocop",
  "version": "1.0.0",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.5.7",
    "prisma": "^5.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node18 --outfile=index.js",
    "start": "ts-node run-locally.ts"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  },
  "dependencies": {
    "@aws-sdk/rds-signer": "^3.405.0",
    "@prisma/client": "^5.2.0"
  }
}
