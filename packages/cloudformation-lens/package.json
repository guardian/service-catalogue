{
  "name": "cloudformation-lens",
  "version": "0.0.1",
  "description": "API for cloudformation stacks",
  "scripts": {
    "test": "go test ./...",
    "build": "go test ./... && env GOOS=linux GOARCH=arm64 go build -o cloudformation-lens main.go",
    "dev": "go run main.go --profile deployTools --in-memory",
    "start": "npm run dev",
    "lint": "go vet ./...",
    "typecheck": "true"
  }
}
